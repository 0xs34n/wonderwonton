# npx create-react-app wonderwonton

# npm run start

If you run `npm run start` in your command line. You will see a development server at `http://localhost:3000`

### Remove boilerplate code

- Removing `logo.svg` in `src/logo.svg` also remove that import in `App.js`
